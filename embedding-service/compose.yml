version: '3.9'
services:
  embedding-service:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8222:8222"
    environment:
      - TZ=Asia/Jakarta
    restart: unless-stopped
